/**
 * Solid color-picker sizing constants (vendored from atom-components'
 * BackgroundPicker, trimmed to the solid editor). Sizes follow the Design
 * System's spacing/height scale.
 */

/** Height of the saturation/value canvas (px) — Figma "Colorspace". */
export const CANVAS_HEIGHT = 97;

/** Height of the hue / alpha tracks (px) — Figma track height. */
export const TRACK_HEIGHT = 16;

/** Diameter of the custom slider thumbs (px) — Figma ellipse. */
export const THUMB_SIZE = 15;

/** Default keyboard step for sliders. */
export const STEP = 1;
export const STEP_LARGE = 10;
